[workspace]
members = ["rofi-websearch"]
resolver = "3"

[profile.release]
lto = "fat"
codegen-units = 1
panic = "abort"
strip = "symbols"

[workspace.dependencies]
rofi-mode = "0.5.0"
rofi-plugin-sys = "0.6.0"
